#pragma once

#include <epoch_core/enum_wrapper.h>

// GLAZE IS ADDED AUTOMATICALLY IN EpochCore.cmake
CREATE_ENUM(WebSocketFeed, Stocks, Options, Forex, Crypto, Indices);

CREATE_ENUM(SortOrder, Asc, Desc);
